# PharmAssist 작업 규칙

## 기능 축소 금지

이 항목이 다른 모든 판단보다 앞선다.

- 사람이 검토해야 한다는 이유로 이미 구현한 기능을 지우거나, 비활성화하거나, 약하게 바꾸지 않는다. 검토 대기는 상태 플래그일 뿐 코드를 덜어낼 근거가 아니다.
- 승인을 기다리는 항목도 기능은 끝까지 구현한다. 플래그만 false로 둔다.
- "약사가 봐야 하는 부분이라 제가 할 수 있는 건 여기까지"라고 범위를 줄이지 않는다. 범위를 줄일지는 사용자가 정한다.
- 정말 막힌 부분이 있으면 나머지를 전부 끝낸 뒤 무엇을 왜 남겼는지 밝힌다. 막혔다는 이유로 착수하지 않는 것은 퇴보다.
- 후보 제품이 없는 프로토콜을 팩에서 빼지 않는다. 검색 가능한 상태로 두고 안전 게이트가 후보 0건을 반환하게 한다. 이 저장소가 이미 negative 용어를 검색 제외가 아니라 의뢰 신호로 처리하는 방식과 같다.

다음은 기능 축소가 아니라 자기승인이므로 그대로 금지한다. 위 규칙이 이것들을 풀어주지 않는다.

- verified, published, pharmacist_reviewed를 임의로 true로 변경
- clinicalUseProhibited 해제
- 공식 레지스트리에 없는 제품을 만들어 채우기
- 테스트를 느슨하게 고쳐 안전 실패를 감추기

## 명명 규칙

- 이미 쓰이는 도메인 용어를 다른 뜻으로 재사용하지 않는다. `inventory`는 `tenant_inventory`의 실제 약국 재고(available_quantity, in_stock/out_of_stock)를 가리킨다. 공식 제품 레지스트리에 제품이 없는 상태를 재고라고 부르지 않는다.

# PharmAssist 상담 UX 원칙

- 환자 대사는 실제 약국 약사가 짧게 말하는 자연스러운 한국어로 작성한다.
- 연령, 임신·수유, 알레르기, 주요 질환, 복용약, 증상 기간을 한 문장에 전부 묻는 보험성 질문을 환자 화면에 노출하지 않는다.
- 내부 작성 지침, 엔진 용어, 스키마 설명, 안전 체크리스트를 환자 대사로 출력하지 않는다.
- 추가 확인이 필요하면 현재 증상과 추천 선택을 실제로 가르는 질문 한 가지만 묻는다.
- 안전 규칙과 red flag 판정은 유지하되, 약사용 내부 점검과 환자에게 할 말을 분리한다.
- 위험 신호가 명확한 경우에만 진료·응급 안내를 표시한다.
- 질문 중에도 임상적으로 구분 가능한 현재 후보가 있으면 적용 조건과 함께 먼저 보여준다.
