{
  "name": "@pharmassist/ingest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --config ../../vitest.package.config.ts"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  },
  "dependencies": {
    "@pharmassist/contracts": "workspace:*",
    "@pharmassist/domain": "workspace:*"
  }
}
